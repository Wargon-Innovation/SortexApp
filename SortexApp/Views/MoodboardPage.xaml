<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:vm="clr-namespace:SortexApp.ViewModels"
             x:Class="SortexApp.Views.MoodboardPage">

    <ContentPage.BindingContext>
        <vm:MoodboardViewModel />
    </ContentPage.BindingContext>



    <ContentPage.Content>
        <StackLayout>
            <CarouselView ItemsSource="{Binding MoodboardList}">
                <CarouselView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout>
                            <Frame>
                                <StackLayout>
                                    <Label Text="{Binding Name}" TextColor="Green" HorizontalOptions="Center"></Label>
                                    <Image x:Name="imgMain" Source="{Binding Image}" WidthRequest="200" HeightRequest="350" VerticalOptions="Center" HorizontalOptions="FillAndExpand"/>
                                    <Label Text="{Binding Description}" TextColor="Green"></Label>

                                </StackLayout>
                            </Frame>
                        </StackLayout>
                    </DataTemplate>
                </CarouselView.ItemTemplate>
            </CarouselView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>